spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/tenant_db
    username: postgres
    password: postgres
  security:
    user:
      name: admin
      password: admin

logging:
  level:
    web: debug